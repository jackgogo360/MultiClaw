[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "multiclaw"
version = "0.1.0"
description = "MultiClaw Agent Runtime"
requires-python = ">=3.12"
dependencies = [
    "aiomysql>=0.2,<1",
    "aiosqlite>=0.20",
    "alembic>=1.13,<2",
    "anyio>=4.0.0",
    "cryptography>=43,<47",
    "ddgs>=9.14.4",
    "html2text>=2025.4.15",
    "httpx>=0.27",
    "mcp>=1.0.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "PyJWT>=2.9",
    "sqlalchemy>=2.0,<3",
    "trafilatura>=2.0.0",
    "websockets>=12.0",
]

[project.scripts]
multiclaw = "multiclaw.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "native_sandbox: opt-in integration tests that exercise real native sandbox backends",
    "macos_sandbox: macOS seatbelt native sandbox integration tests",
    "linux_sandbox: Linux nsjail native sandbox integration tests",
]
