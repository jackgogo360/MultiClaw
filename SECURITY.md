# 安全策略

MultiClaw 处理模型输出、工具调用、租户数据和用户提供的凭据，安全问题可能影响主机、数据隔离或外部服务。请负责任地报告疑似漏洞。

## 支持范围

项目当前处于 `0.1.0` 开发阶段，尚未正式发布。

| 版本 | 安全修复状态 |
|---|---|
| 当前 `master` 及活跃开发分支 | 尽力修复 |
| 历史提交、个人分支和修改版 | 不提供维护承诺 |

开发阶段不承诺固定响应时限或向后兼容；高风险问题仍会优先评估和处理。

## 私密报告漏洞

请使用仓库的 [GitHub Security Advisories](https://github.com/jackgogo360/MultiClaw/security/advisories/new) 创建私密报告。不要为未公开漏洞创建公开 Issue、Discussion 或 Pull Request。

如果私密报告入口不可用，请只在公开 Issue 中说明“需要安全联系渠道”，不要披露复现步骤、敏感日志或利用代码。

## 报告内容

请尽量包含：

- 受影响的提交、版本、平台和配置。
- 问题类型与可能影响。
- 最小复现步骤或安全的概念验证。
- 预期行为与实际行为。
- 已尝试的缓解方式。
- 是否已被利用、公开或报告给其他项目。

所有示例必须删除令牌、邮箱、数据库连接串、租户标识、工作区路径和真实业务数据。

## 响应流程

维护者会确认报告范围、复现问题、评估严重程度并协调修复与披露。在修复可用前，请不要公开细节。修复合并后，维护者将根据影响决定是否发布安全公告、升级说明或密钥轮换建议。

## 敏感信息

- 仓库不应包含 API key、JWT 签名密钥、Secret keyring、私钥、`.env`、数据库或运行日志。
- 如果凭据曾进入 Git 历史，应先在外部服务旋转或吊销，再评估历史重写。
- 不要把 Secret 放入 URL、异常、日志、SSE、checkpoint、审计详情、指标标签或截图。
- 不要通过关闭 CSRF、就绪性、租户作用域或生产沙箱来规避故障。

## 安全设计

项目的信任边界、租户隔离、Secret envelope、MCP 权限、原生沙箱和已知限制见[安全模型](docs/security-model.md)。部署前还应阅读[部署指南](docs/deployment.md)与[沙箱部署指南](docs/sandbox-deployment.md)。
